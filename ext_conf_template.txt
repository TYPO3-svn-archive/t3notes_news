
  # cat=basic//10; type=text; label= News detail base url: URL for the news detail content links
newsDetailBaseUrl = 

  # cat=basic//10; type=text; label= News detail replace list: List of HTML attributes to replace
newsDetailReplaceList = src=",url=",href="

  # cat=basic//10; type=text; label= List of valid file types: List of valid file type endings
newsValidFileExtension = jpg,jpeg,gif,xls,ppt,doc,pdf

  # cat=basic//10; type=text; label= Server unreachable error message: Message to show if the Lotus Notes server is unreachable
serverUnreachableErrorMessage = Der Dienst steht derzeit nicht zur Verfügung. Bitte versuchen Sie es später erneut.
